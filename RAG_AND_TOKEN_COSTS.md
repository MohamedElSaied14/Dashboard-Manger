# RAG على الـ PDF + توفير التوكينز + شريط التحميل

ثلاث حاجات اتعملت: تقليل استهلاك التوكينز في مراجعة التصميم، شريط تحميل بنسبة مئوية حقيقية،
ونظام RAG لملفات الـ PDF.

---

## 1) توفير التوكينز (Token savings)

### كان بيحصل إيه قبل كده

مراجعة تصميم واحدة كانت بتعمل حوالي **20 نداء** على الـ vision model، وكل نداء بيبعت صورة التصميم
كاملة بـ `detail: "high"`، وكمان:

- الـ general review كان بيبعت كل صور الـ references — ونفس الصور دي كانت بتتبعت **تاني** في
  `verifyReferences`، يعني دفع مرتين لنفس الصورة.
- كل رقم/بيانات تواصل محفوظة (لحد 8) كان ليها نداء لوحدها بصورتين high-detail = 8 نداءات.
- كل لوجو كان بياخد نداءين (فحص + identity gate) حتى لو النتيجة الأولى قاطعة.
- الـ prompt كان فيه تعليمات كاملة عن اللوجوهات والأرقام والـ references رغم إن كل واحدة منهم
  ليها pass مستقل بيتعامل معاها.
- الصور كانت بتتبعت بحجمها الأصلي، والـ crops كانت PNG بعرض 1600px.
- **مفيش أي قياس** لعدد التوكينز، ومفيش سقف — مراجعة واحدة ممكن تصرف من غير حد.

### اللي اتعمل

| التغيير | المكان |
|---|---|
| صور الـ references اتشالت من الـ general call (بتتبعت مرة واحدة بس في الـ pass المخصص) | `ai-review.service.ts` |
| كل فحوصات بيانات التواصل بقت **نداء واحد** بدل نداء لكل رقم | `verifyContacts` |
| الـ identity gate بيتنادى بس لما ممكن يغيّر النتيجة | `verifyLogos` |
| كل صورة بتتجاب **مرة واحدة** للمراجعة كلها وبتتصغّر لـ 1024px JPEG | `image-prep.util.ts` |
| الـ crops بقت JPEG بعرض 1100px بدل PNG 1600px | `createLogoRegionCrop` / `createExpectedRegionCrop` |
| الـ prompts بقت compact JSON من غير مسافات زيادة، والبيانات اللي الـ pass مش شايفها اتشالت | `compactJson` / `slimGuidelines` |
| `max_tokens` بقى لكل نداء على حسب حجمه بدل 3500 لكل نداء | `createVisionJson` |
| **قياس فعلي + سقف** لكل مراجعة | `token-budget.util.ts` |

#### السقف والقياس

كل مراجعة بتعمل `TokenMeter` بتسجل الاستهلاك الحقيقي اللي راجع من OpenAI، وبترفض تبدأ نداء جديد
لو الميزانية خلصت. لما ده يحصل الفحوصات الباقية بترجع `unknown` + "manual review required" —
يعني بتقف بأمان، مش بتصرف من غير حد.

الاستهلاك بيتسجل في:

- **اللوجز**: `design-review: 38412 tokens over 7 calls (prompt 36900, cached 0, completion 1512, skipped 0, budget 120000) :: logo-check=18200(3) general-review=9800(1) ...`
- **قاعدة البيانات**: `DesignReview.aiAnalysis.usage` — يعني تقدر تحاسب كل تصميم بتكلفته.

#### متغيرات البيئة الجديدة (كلها اختيارية وليها defaults)

```bash
OPENAI_REVIEW_TOKEN_BUDGET=120000   # سقف التوكينز للمراجعة الواحدة (0 = بدون سقف)
OPENAI_VISION_MAX_IMAGE_EDGE=1024   # أطول ضلع للصورة قبل ما تتبعت للموديل
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_EMBEDDING_BATCH_SIZE=96
OPENAI_RAG_MODEL=                   # لو فاضي بيستخدم OPENAI_DESIGN_REVIEW_MODEL
RAG_MAX_CANDIDATES=20000            # أقصى عدد جُمل بيتم مسحها في البحث الواحد
```

> **دقة المراجعة اتحافظ عليها.** التقليل كله في التكرار (نفس الصورة مبعوتة مرتين، نداء لكل رقم،
> gate مش هيغيّر النتيجة) وفي حجم البيانات — مش في الأدلة اللي الموديل بيشوفها. كل حواجز الأمان
> اللي كانت موجودة (confidence gates، التحقق المستقل من هوية اللوجو، `unknown` بدل التخمين) زي ما هي.

---

## 2) شريط التحميل بنسبة مئوية

`fetch()` **مش** بيقدر يقيس تقدم رفع الملف — عشان كده كل الرفع في التطبيق كان بيقول
"Uploading..." وخلاص. الحل: `apps/web/src/utils/apiUpload.ts` بيستخدم `XMLHttpRequest` لأنه
الوحيد اللي بيدي `upload.onprogress` في المتصفح.

النسبة بتتقسم على 3 مراحل عشان الرفع بياخد وقت على السيرفر بعد ما البايتس توصل:

| المرحلة | النسبة | مقيسة ولا لأ |
|---|---|---|
| `uploading` | 0–90% | مقيسة بالبايت الحقيقي + عرض `2.4 MB / 5.1 MB` |
| `processing` | 90–99% | Cloudinary / قراءة الـ PDF / الفهرسة — بتتحرك تدريجيًا |
| `done` | 100% | وصل الرد |

الاستخدام:

```tsx
const progress = useUploadProgress();
apiUpload("/clients/x/designs", { body: formData, onProgress: progress.onProgress });
// ...
{mutation.isPending && <UploadProgressBar progress={progress.progress} />}
```

اتربط في: رفع التصميم، رفع الـ reference، رفع اللوجو (في مكانين)، رفع الـ PDF للبريف، وفهرسة
مستندات قاعدة المعرفة.

---

## 3) نظام RAG للـ PDF

`apps/api/src/rag/` — فهرسة الملف **مرة واحدة**، وبعدين كل سؤال بيقرأ المقاطع المتعلقة بيه بس.

### الطريقة: Sentence-window retrieval + Auto-merging

ده تطبيق للأسلوبين الأساسيين في كورس *Building and Evaluating Advanced RAG*:

1. **اللي بيتعمل له embedding = جملة واحدة.** الجملة صغيرة ومحددة فالمطابقة بتبقى دقيقة، عكس
   ما لو عملنا embedding لصفحة كاملة فيها 5 مواضيع.
2. **اللي بيتبعت للموديل = الجملة + اللي حواليها** (`windowSize`، افتراضي ±2 جملة). الموديل بيقرأ
   فكرة كاملة مش شظية. النافذة بتتبني وقت السؤال من `sentenceIndex` — مش متخزنة، عشان ما نكررش
   نفس النص 3 مرات في قاعدة البيانات.
3. **Auto-merging**: لو أكتر من نتيجة وقعت في نفس القسم (`parentIndex`)، بيرجع القسم كامل مرة
   واحدة بدل 3 مقاطع متداخلة.
4. **Hybrid scoring**: `0.8 × cosine + 0.2 × تطابق الكلمات الحرفي` — التطابق الحرفي مهم عشان
   رقم تليفون أو كود لون `#1A2B3C` ما يضيعش في إعادة الصياغة.
5. **سقف على الـ context** (`maxContextChars`, افتراضي 6000 حرف) — ده الزرار اللي بيحدد تكلفة
   السؤال الواحد فعليًا.

### التقسيم (Chunking)

`chunking.util.ts` بيشتغل عربي وإنجليزي:

- بيصلّح الكلمات المقطوعة على سطرين في الـ PDF (`guide-\nlines` → `guidelines`).
- بيفصل على `. ! ? ؟ …` وعلى الأسطر (عشان النقط والعناوين).
- **العنوان لوحده مش جملة**: "الألوان" بتفضل ملزوقة بالقاعدة اللي تحتها، مش متفهرسة كضوضاء.
- السطر الطويل جدًا بينقسم عند أقرب مسافة عشان جملة واحدة ما تسيطرش على النافذة.
- رقم الصفحة محفوظ لكل جملة → كل إجابة ليها مصدر.

### الـ Endpoints

```
POST   /api/clients/:clientId/knowledge/documents            # فهرسة PDF (multipart) — admin/manager
GET    /api/clients/:clientId/knowledge/documents            # قائمة المستندات وحالتها
GET    /api/clients/:clientId/knowledge/documents/:id
POST   /api/clients/:clientId/knowledge/documents/:id/reindex
DELETE /api/clients/:clientId/knowledge/documents/:id
GET    /api/clients/:clientId/knowledge/search?q=...         # المقاطع بس، من غير موديل — للتشخيص
POST   /api/clients/:clientId/knowledge/ask                  # إجابة + مصادر + تكلفة
```

الرد على `ask` بيرجع الإجابة، الاقتباسات (اسم المستند + رقم الصفحة + النص)، وعدد التوكينز.
لو مفيش مقاطع عدّت الحد الأدنى، بيرجع "المستندات مفيهاش إجابة" **من غير** ما ينادي الموديل أصلًا.

### الربط مع استخراج البريف

`extractBrief` و `extractGuidelinesDraft` بقوا:

- PDF **أصغر من 6000 حرف** → بيتبعت كامل زي الأول (الفهرسة هتكلف أكتر من إنك تبعته).
- PDF **أكبر من كده** → بيتفهرس مرة واحدة، وبعدين بيتم استرجاع المقاطع الخاصة بـ 8 مواضيع
  (الألوان، الخطوط، اللوجو، بيانات التواصل، نبرة الصوت، المقاسات، الممنوعات، نبذة عن العميل) —
  كل موضوع مكتوب بالعربي والإنجليزي عشان يطابق المستندات المختلطة. بيتبعت للموديل 9000 حرف كحد
  أقصى بدل الملف كله.
- لو الاسترجاع رجع حاجة ضعيفة (أقل من 1000 حرف) بيرجع للنص الكامل — الفهرسة تحسين، مش بوابة.
- لو الفهرسة فشلت خالص، الاستخراج بيكمل عادي بالنص الكامل.

الرد بقى فيه `source` بيقول `mode: "retrieved" | "full-text"` وكام حرف اتبعت فعلًا من كام حرف أصلي.

### التكرار مجاني

الفهرسة مربوطة بـ **checksum** للمحتوى. لو رفعت نفس الملف تاني بيرجع الفهرس الموجود
(`reused: true`) من غير ما يدفع embeddings تاني.

### الواجهة

تبويب **"قاعدة المعرفة"** جديد في صفحة العميل (`/clients/[id]`): رفع الـ PDF بشريط النسبة،
قائمة المستندات وحالتها، وصندوق أسئلة بيعرض الإجابة + المصادر بالصفحات + تكلفة السؤال بالتوكينز.

---

## الاختبارات

```bash
cd apps/api && npx jest
```

`src/rag/rag.spec.ts` بيغطي: تقسيم الجمل عربي/إنجليزي، لصق العنوان بالنص، تقسيم السطر الطويل،
ترقيم الجمل عبر الصفحات، الـ tokenizer، حساب cosine، دمج النوافذ المتداخلة، وسقف التوكينز.
